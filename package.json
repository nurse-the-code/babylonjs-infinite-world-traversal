{
  "name": "babylonjs-infinite-world-traversal",
  "license": "(MIT OR Apache-2.0)",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "prepare": "pnpm build && husky install",
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test:once": "jest",
    "test:watch": "jest --watch",
    "format:write": "prettier . --write",
    "format:check": "prettier . --check",
    "nuke": "rm -rf node_modules dist pnpm-lock.yaml yarn.lock package-lock.json && pnpm install"
  },
  "devDependencies": {
    "@babylonjs/materials": "^7.1.0",
    "@types/jest": "^29.5.12",
    "husky": "^9.0.11",
    "jest": "^29.7.0",
    "prettier": "3.2.5",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.3",
    "vite": "^5.2.8"
  },
  "dependencies": {
    "@babylonjs/core": "^7.1.0"
  }
}
